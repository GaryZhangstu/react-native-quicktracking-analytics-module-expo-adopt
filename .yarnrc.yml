# Use classic node_modules approach for compatibility
nodeLinker: node-modules

# Display progress bars for better UX
enableProgressBars: true

# Use the same npm registry
npmRegistryServer: "https://registry.npmjs.org"
